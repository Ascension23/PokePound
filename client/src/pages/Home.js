import React from 'react';
import { useQuery } from '@apollo/client';

const Home = () => {

  return (
    <main>
      <div>
        this is the home page . . .
      </div>
    </main>
  );
};

export default Home;
